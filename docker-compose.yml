version: '3'

services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - MYSQL_HOST=190.171.167.226
      - MYSQL_PORT=3306
      - MYSQL_USER=root
      - MYSQL_PASSWORD=sersoft
      - MYSQL_DATABASE=BDCrispieri2019
      - SERVER_PORT=8080
      - GIN_MODE=release
